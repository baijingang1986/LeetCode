public class Solution {
    public int singleNumber(int[] A) {
        int n = A.length;
        int ones = 0;
        for(int i = 0;i<n;i++){
            ones = ones ^ A[i];
        }
        return ones;
    }
}
